import { CapacitorConfig } from '@capacitor/cli';
const config: CapacitorConfig = {
  appId: "io.ionic.othavio.demo.pg.cap.ng",
  appName: "Othavio - Desafio Mob. GS3",
  bundledWebRuntime: false,
  npmClient: "npm",
  webDir: "www",
};

export default config;
